import React from 'react';
import { View, Text } from 'react-native';

const Share = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Share</Text>
    </View>
  );
};

export default Share;
